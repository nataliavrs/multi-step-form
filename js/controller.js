import navigationBarView from "./Views/navigationBarView";

const goToNextStep = function () {
  // save data state
  // navigate route
  // render UI
};

const goBack = function () {
  // get data
  // change route
  // render UI
};

const init = function () {
  navigationBarView.addHandlerNavigateNext(goToNextStep);
};

init();
